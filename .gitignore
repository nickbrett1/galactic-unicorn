# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# SvelteKit
.svelte-kit
build
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OS
.DS_Store
Thumbs.db

# Antigravity
.antigravitycli

# Optional


# Python
__pycache__/
*.py[cod]
*$py.class
.venv
venv/
*.manifest
*.egg-info/




# Device-local secrets. config.py is committed and holds no credentials;
# WiFi lives in config_secrets.py, which never enters the repo (memo Appendix C).
config_secrets.py

# Ruff
.ruff_cache/

# Firmware images (large; fetched from pimoroni/unicorn releases)
*.uf2

# Original stock Pimoroni demo pulled off the board
.board-backup/

# Written on the device by lib/updater.py, never in the repo
version.txt
update.log

# Written on the device by the phase-2 remote poller / the crash handler
# (lib/remote.py, main.py): board-local state, never in the repo and never in
# the update pack (the pack ships only main.py, config.py, routines.json and
# lib/*.py - scripts/build-firmware-pack.py).
remote.log
applied_gen.txt
crash.log

# Python bytecode from the host-side harnesses
__pycache__/

# Audition renders from scripts/render-fanfare.py: a way to HEAR a note list
# with no board on the wire, not firmware and not an artifact.
*.wav

# device-written OTA state
boot-ok.txt
boot-try.txt
bad.txt
prev.json
